<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.tejeet.saiwatersuppliers">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:name=".MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SaiWaterSuppliers">
        <activity android:name=".Ui.Activity.AddUserActivity"/>
        <activity android:name=".Ui.Activity.SettingActivity" />
        <activity android:name=".Ui.Activity.DriversActivity" />
        <activity android:name=".Ui.Activity.SocietiesActivity" />
        <activity android:name=".Ui.Activity.MainActivity"/>
        <activity android:name=".Login"/>
        <activity android:name=".FirstScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>